# Core ML / Fine-tuning
unsloth>=2024.12.0
transformers>=4.46.0
datasets>=3.0.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
torch>=2.4.0
xformers>=0.0.28
sentencepiece>=0.2.0
protobuf>=5.0.0

# Web scraping
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Existing project deps
# Stealth engine: ShardX anti-detect browser (ProxyShard ShardBrowser) —
# fingerprints ALWAYS randomized per session: fresh profile from the 170-profile
# fingerprint library + randomized hardware/platform version per launch,
# driven over CDP by patchright (stealth Playwright).
shardx>=0.1.11
patchright>=1.60.0
# Legacy engines (kept for git history / one-off scripts):
# truedriver>=0.1.5
# clearcote>=0.25.0
Pillow>=10.0.0
asyncpg>=0.29.0
flask>=3.0.0

# Captcha solver deps (brain + OpenCV + TLS)
opencv-python-headless>=4.8.0
numpy>=1.24.0
curl_cffi>=0.7.0
torch>=2.0.0
torchvision>=0.15.0

# Email checker (MX record lookups; optional - falls back to plain DNS)
dnspython>=2.6.0
