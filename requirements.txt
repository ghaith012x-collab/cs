# Core ML / Fine-tuning
unsloth>=2024.12.0
transformers>=4.46.0
datasets>=3.0.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
torch>=2.4.0
xformers>=0.0.28
sentencepiece>=0.2.0
protobuf>=5.0.0

# Web scraping
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Existing project deps
# Stealth engine: SeleniumBase (https://github.com/seleniumbase/SeleniumBase)
# in CDP Mode (Chrome DevTools Protocol — the stealth successor to UC Mode),
# driving the BRAVE browser (unbranded Chromium) with incognito ALWAYS on.
# CDP Mode launches with the UC (undetected-chromedriver) driver, then
# activate_cdp_mode() disconnects chromedriver and re-drives the browser over
# a raw CDP websocket: no WebDriver is attached while navigating or clicking
# Cloudflare Turnstile widgets. cdp_click / uc_click / uc_gui_click_captcha
# handle CAPTCHAs at the CDP/OS level instead of injecting JS, so Cloudflare
# can't fingerprint synthetic events.
seleniumbase>=4.51.0
# (Legacy ShardX/patchright engines removed from the active path — kept only
# for the old shardx_engine.py reference + Dockerfile pre-fetch. Repo default
# is SeleniumBase.)
shardx>=0.1.11
patchright>=1.60.0
Pillow>=10.0.0
asyncpg>=0.29.0
flask>=3.0.0

# Captcha solver deps (brain + OpenCV + TLS)
opencv-python-headless>=4.8.0
numpy>=1.24.0
curl_cffi>=0.7.0
torch>=2.0.0
torchvision>=0.15.0

# Email checker (MX record lookups; optional - falls back to plain DNS)
dnspython>=2.6.0
